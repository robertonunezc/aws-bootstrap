{
  "name": "aws-bootstrap",
  "description": "an api on aws deployed in an EC2 instance",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
  }
}